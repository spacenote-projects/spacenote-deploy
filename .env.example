# Domain configuration
DOMAIN=example.com
LETSENCRYPT_EMAIL=admin@example.com

# MongoDB configuration
MONGO_ROOT_USERNAME=root
MONGO_ROOT_PASSWORD=changeme_root_password

# Backend configuration
JWT_SECRET_KEY=changeme_jwt_secret_key_must_be_at_least_32_chars_long

# Docker images (optional, defaults to local builds)
BACKEND_IMAGE=spacenote-backend:latest
FRONTEND_IMAGE=spacenote-frontend:latest

# Debug mode (set to true for development)
DEBUG=false