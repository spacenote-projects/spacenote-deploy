# Domain configuration
DOMAIN=example.com
EMAIL=admin@example.com  # Optional: for Let's Encrypt notifications

# MongoDB configuration
MONGODB_ROOT_USERNAME=root
MONGODB_ROOT_PASSWORD=changeme_root_password

# Backend configuration
SESSION_SECRET_KEY=changeme_session_secret_key_must_be_at_least_32_chars_long
TELEGRAM_BOT_TOKEN=  # Optional: Telegram Bot API token for notifications

# Docker images
BACKEND_IMAGE=ghcr.io/spacenote-projects/spacenote-backend:latest
FRONTEND_IMAGE=ghcr.io/spacenote-projects/spacenote-frontend:latest
