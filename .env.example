# Domain configuration
DOMAIN=example.com
EMAIL=admin@example.com  # Optional: for Let's Encrypt notifications

# MongoDB configuration
MONGODB_ROOT_USERNAME=root
MONGODB_ROOT_PASSWORD=changeme_root_password

# Backend configuration
SESSION_SECRET_KEY=changeme_session_secret_key_must_be_at_least_32_chars_long

# Docker images (build locally or use your registry)
# Default: local builds (spacenote-backend:latest, spacenote-frontend:latest)
# Example for GitHub registry: ghcr.io/your-username/spacenote-backend:latest
#BACKEND_IMAGE=spacenote-backend:latest
#FRONTEND_IMAGE=spacenote-frontend:latest

# Debug mode (set to true for development)
DEBUG=false